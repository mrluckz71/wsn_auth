[project]
name = "wsn-auth-project"
version = "0.1.0"
description = "WSN authentication protocol simulation for experimentation."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
